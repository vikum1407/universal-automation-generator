import { test, expect } from '@playwright/test';
import { apiClient } from '../utils/apiClient';

test('{{name}}', async () => {
  const client = apiClient();
  const response = await client.{{methodLower}}('{{path}}');
  expect(response.status()).toBe({{expectedStatus}});
});
