# Cypress Cloud — {{PROJECT_NAME}}

> Generated by **Qlitz Framework Generator** · Cypress Cloud Parallelisation

## Setup

1. [Create a Cypress Cloud account](https://cloud.cypress.io/login)
2. Add your Project ID to `cypress.config.ts`:

```ts
export default defineConfig({
  projectId: 'your-project-id-here',
  // ... rest of config
});
```

3. Add your Record Key to CI secrets as `CYPRESS_RECORD_KEY`.

## Run with Parallelisation

```bash
# Local (record only)
npx cypress run --record --key $CYPRESS_RECORD_KEY

# CI — split across 4 machines
npx cypress run --record --parallel --key $CYPRESS_RECORD_KEY
```

## GitHub Actions Example

```yaml
- name: Run Cypress on Cloud
  uses: cypress-io/github-action@v6
  with:
    record: true
    parallel: true
    group: '{{PROJECT_NAME}} — Chrome'
  env:
    CYPRESS_RECORD_KEY: ${{ secrets.CYPRESS_RECORD_KEY }}
    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

---
_Generated {{YEAR}} · {{AUTHOR}}_
