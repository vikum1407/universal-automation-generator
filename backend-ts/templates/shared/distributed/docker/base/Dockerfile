# Generated by Qlitz Framework Generator
# {{PROJECT_NAME}} — Selenium + Java test runner image

# ── Stage 1: dependency cache ──────────────────────────────────────────────────
FROM maven:3.9.6-eclipse-temurin-17 AS deps
WORKDIR /app
COPY pom.xml .
RUN mvn dependency:go-offline -B -q

# ── Stage 2: runtime image ────────────────────────────────────────────────────
FROM maven:3.9.6-eclipse-temurin-17

LABEL org.opencontainers.image.title="{{PROJECT_NAME}}"
LABEL org.opencontainers.image.description="Generated by Qlitz Framework Generator"
LABEL org.opencontainers.image.version="{{VERSION}}"

# Install Chrome
RUN apt-get update -q && apt-get install -yq \
    wget gnupg ca-certificates curl \
    && wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | gpg --dearmor -o /usr/share/keyrings/google-chrome.gpg \
    && echo "deb [arch=amd64 signed-by=/usr/share/keyrings/google-chrome.gpg] http://dl.google.com/linux/chrome/deb/ stable main" \
       > /etc/apt/sources.list.d/google-chrome.list \
    && apt-get update -q && apt-get install -yq google-chrome-stable \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# Restore dependency cache
COPY --from=deps /root/.m2 /root/.m2

# Copy project
COPY . .

COPY entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh

ENV BROWSER={{BROWSER}}
ENV BASE_URL={{BASE_URL}}
ENV HEADLESS=true

ENTRYPOINT ["/entrypoint.sh"]
