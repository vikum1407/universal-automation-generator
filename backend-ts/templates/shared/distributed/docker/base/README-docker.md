# Docker — {{PROJECT_NAME}}

> Generated by **Qlitz Framework Generator** · Docker execution layer

## Build & Run

```bash
# Build the image
docker build -t {{DOCKER_IMAGE_NAME}}:{{DOCKER_IMAGE_TAG}} .

# Run tests (headless)
docker run --rm \
  -e BASE_URL={{BASE_URL}} \
  -e BROWSER={{BROWSER}} \
  -e HEADLESS=true \
  -v "$(pwd)/allure-results:/app/allure-results" \
  {{DOCKER_IMAGE_NAME}}:{{DOCKER_IMAGE_TAG}}

# Run with overrides
docker run --rm \
  -e BASE_URL=https://staging.example.com \
  -e BROWSER=firefox \
  {{DOCKER_IMAGE_NAME}}:{{DOCKER_IMAGE_TAG}}
```

## Environment Variables

| Variable       | Default            | Description           |
|----------------|--------------------|-----------------------|
| `BASE_URL`     | `{{BASE_URL}}`     | Application base URL  |
| `BROWSER`      | `{{BROWSER}}`      | Browser to use        |
| `HEADLESS`     | `true`             | Headless mode         |
| `THREAD_COUNT` | `{{THREAD_COUNT}}` | Parallel thread count |

---
_Generated {{YEAR}} · {{AUTHOR}}_
