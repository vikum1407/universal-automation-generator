# Sauce Labs — {{PROJECT_NAME}}

> Generated by **Qlitz Framework Generator** · Sauce Labs Cloud Execution

## Prerequisites

1. [Sign up for Sauce Labs](https://accounts.saucelabs.com/am/XUI/#register/)
2. Get your credentials from the [User Settings](https://accounts.saucelabs.com/am/XUI/#profile/)
3. Install the [Sauce Labs CLI (saucectl)](https://docs.saucelabs.com/dev/cli/saucectl/)

## Setup

```bash
# Export credentials (or add to CI secrets)
export SAUCE_USERNAME="your-username"
export SAUCE_ACCESS_KEY="your-access-key"

# Validate config
saucectl configure

# Run tests
saucectl run --config infra/cloud/saucelabs-config.yml
```

## CI (GitHub Actions)

```yaml
- name: Run on Sauce Labs
  env:
    SAUCE_USERNAME: ${{ secrets.SAUCE_USERNAME }}
    SAUCE_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
  run: saucectl run --config infra/cloud/saucelabs-config.yml
```

---
_Generated {{YEAR}} · {{AUTHOR}}_
