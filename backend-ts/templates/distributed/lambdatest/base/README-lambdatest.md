# LambdaTest — {{PROJECT_NAME}}

> Generated by **Qlitz Framework Generator** · LambdaTest Cloud Execution

## Prerequisites

1. [Sign up for LambdaTest](https://accounts.lambdatest.com/register)
2. Get your credentials from the [LambdaTest Dashboard](https://accounts.lambdatest.com/dashboard)

## Setup

```bash
# Export credentials (or add to CI secrets)
export LT_USERNAME="your-username"
export LT_ACCESS_KEY="your-access-key"

# Install HyperExecute CLI
curl -Lo ./hyperexecute https://downloads.lambdatest.com/hyperexecute/darwin/amd64/0.2.0/hyperexecute
chmod +x ./hyperexecute

# Run tests
./hyperexecute --config infra/cloud/lambdatest-config.yml \
               --user $LT_USERNAME \
               --key $LT_ACCESS_KEY
```

## CI (GitHub Actions)

```yaml
- name: Run on LambdaTest
  env:
    LT_USERNAME: ${{ secrets.LT_USERNAME }}
    LT_ACCESS_KEY: ${{ secrets.LT_ACCESS_KEY }}
  run: |
    ./hyperexecute --config infra/cloud/lambdatest-config.yml \
                   --user $LT_USERNAME --key $LT_ACCESS_KEY
```

---
_Generated {{YEAR}} · {{AUTHOR}}_
