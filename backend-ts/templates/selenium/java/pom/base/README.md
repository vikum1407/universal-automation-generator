# {{PROJECT_NAME}}

> Generated by **Qlitz Framework Generator** · {{FRAMEWORK}} + {{LANGUAGE}}

## Stack

| Component | Version |
|-----------|---------|
| Framework | Selenium WebDriver 4.x |
| Language  | Java 17 |
| Test Runner | TestNG 7.x |
| Build Tool | Maven 3.x |
| Reporting | {{REPORTING_TOOL}} |

## Quick Start

```bash
# Run all tests
mvn test

# Run with specific browser
mvn test -Dbrowser=firefox

# Run headless
mvn test -Dheadless=true

# Run specific suite
mvn test -DsuiteXmlFile=src/test/resources/testng.xml
```

## Project Structure

```
src/
├── main/java/{{BASE_PACKAGE}}/
│   ├── base/
│   │   ├── BasePage.java       ← Abstract page base class
│   │   ├── BaseTest.java       ← TestNG base test with lifecycle
│   │   └── DriverManager.java  ← Thread-safe WebDriver factory
│   ├── config/
│   │   └── TestConfig.java     ← Properties reader
│   ├── pages/                  ← Add your page objects here
│   └── utils/
│       └── WaitUtils.java      ← Fluent wait utilities
└── test/
    ├── java/{{BASE_PACKAGE}}/
    │   └── tests/
    │       └── SampleTest.java ← Starter test
    └── resources/
        ├── testng.xml          ← Test suite configuration
        └── config.properties   ← Runtime configuration
```

## Configuration

Edit `src/test/resources/config.properties` or pass JVM args:

```properties
base.url={{BASE_URL}}
browser={{BROWSER}}
headless={{HEADLESS}}
thread.count={{THREAD_COUNT}}
```

---
_Generated {{YEAR}} · {{AUTHOR}}_
