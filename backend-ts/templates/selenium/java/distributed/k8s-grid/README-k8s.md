# Kubernetes + Selenium Grid — {{PROJECT_NAME}}

> Generated by **Qlitz Framework Generator** · Kubernetes Grid Execution

## Prerequisites

- `kubectl` configured for your cluster
- Docker registry access to push `{{K8S_IMAGE_NAME}}:{{K8S_IMAGE_TAG}}`

## Deploy

```bash
# 1. Build and push the test runner image
docker build -t {{K8S_IMAGE_NAME}}:{{K8S_IMAGE_TAG}} .
docker push {{K8S_IMAGE_NAME}}:{{K8S_IMAGE_TAG}}

# 2. Create namespace
kubectl apply -f infra/k8s/namespace.yaml

# 3. Deploy Grid
kubectl apply -f infra/k8s/configmap.yaml
kubectl apply -f infra/k8s/selenium-hub-deployment.yaml
kubectl apply -f infra/k8s/selenium-hub-service.yaml
kubectl apply -f infra/k8s/selenium-node-deployment.yaml

# 4. Wait for Grid to be ready
kubectl wait --for=condition=available deployment/{{GRID_SERVICE_NAME}} \
  -n {{K8S_NAMESPACE}} --timeout=120s

# 5. Run tests
kubectl apply -f infra/k8s/test-runner-job.yaml

# 6. Watch logs
kubectl logs -f job/{{PROJECT_NAME}}-tests -n {{K8S_NAMESPACE}}

# 7. Clean up
kubectl delete -f infra/k8s/
```

## Configuration

| Setting             | Value                         |
|---------------------|-------------------------------|
| Namespace           | `{{K8S_NAMESPACE}}`           |
| Grid service        | `{{GRID_SERVICE_NAME}}`       |
| Chrome node replicas| `{{K8S_NODE_REPLICAS}}`       |
| Test runner image   | `{{K8S_IMAGE_NAME}}:{{K8S_IMAGE_TAG}}` |
| Grid URL (in-cluster)| `http://{{GRID_SERVICE_NAME}}.{{K8S_NAMESPACE}}.svc.cluster.local:4444` |

---
_Generated {{YEAR}} · {{AUTHOR}}_
