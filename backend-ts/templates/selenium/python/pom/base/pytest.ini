[pytest]
# {{PROJECT_NAME}} — pytest configuration
# Framework: {{FRAMEWORK}} | Language: {{LANGUAGE}}
# Generated by Qlitz Framework Generator

testpaths = tests
addopts =
    -v
    --tb=short
    --strict-markers
    --html=reports/report.html
    --self-contained-html

markers =
    smoke: Smoke test suite
    regression: Regression test suite
    ui: UI tests
    api: API tests
    hybrid: Hybrid tests
