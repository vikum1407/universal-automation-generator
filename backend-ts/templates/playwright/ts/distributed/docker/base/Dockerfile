# Generated by Qlitz Framework Generator
# {{PROJECT_NAME}} — Playwright + TypeScript test runner image

FROM mcr.microsoft.com/playwright:v1.43.0-noble

LABEL org.opencontainers.image.title="{{PROJECT_NAME}}"
LABEL org.opencontainers.image.description="Generated by Qlitz Framework Generator"
LABEL org.opencontainers.image.version="{{VERSION}}"

WORKDIR /app

# Install dependencies first (cache layer)
COPY package*.json ./
RUN npm ci

# Copy project files
COPY . .

ENV BASE_URL={{BASE_URL}}
ENV BROWSER={{BROWSER}}
ENV CI=true

CMD ["npx", "playwright", "test", "--reporter=html"]
